1)
SELECT
    E.FIRST_NAME,
    E.LAST_NAME,
    T.NAME,
    A.ADDRESS_TEXT
FROM
    EMPLOYEES AS E
INNER JOIN
    ADDRESSES AS A ON E.ADDRESS_ID = A.ADDRESS_ID
INNER JOIN
    TOWNS AS T ON A.TOWN_ID = T.TOWN_ID
ORDER BY
    E.FIRST_NAME ASC,
    E.LAST_NAME ASC
LIMIT 5;
2)

